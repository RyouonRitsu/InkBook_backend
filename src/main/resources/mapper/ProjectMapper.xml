<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ryouonritsu.inkbook_backend.dao.ProjectDao">
    <insert id="createNewProject" parameterType="Project" useGeneratedKeys="true" keyProperty="team_id">
        INSERT INTO `project`(`project_name`, `project_info`, `team_id`)
        VALUES (#{project_name}, #{project_info}, #{team_id})
    </insert>
    <select id="searchTeamIdByProjectId" resultType="String">
        SELECT team_id
        FROM `project`
        WHERE project_id = #{project_id}
    </select>
    <delete id="deleteProject">
        DELETE
        FROM `project`
        WHERE project_id = #{project_id}
    </delete>
    <update id="updateProject" parameterType="String">
        UPDATE `project`
        SET project_name = #{project_name}, project_info = #{project_info}
        WHERE project_id = #{project_id}
    </update>
    <select id="searchProjectByTeamId" resultType="Map">
        SELECT *
        FROM `project`
        WHERE team_id = #{team_id}
    </select>
</mapper>